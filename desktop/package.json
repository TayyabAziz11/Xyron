{
  "name": "ai-operator-desktop",
  "version": "0.1.0",
  "description": "AI Operator Desktop Companion",
  "main": "dist/main.js",
  "scripts": {
    "dev": "tsc && DBUS_SESSION_BUS_ADDRESS=autolaunch: electron --no-sandbox dist/main.js",
    "build": "tsc",
    "start": "DBUS_SESSION_BUS_ADDRESS=autolaunch: electron --no-sandbox dist/main.js"
  },
  "devDependencies": {
    "electron": "^36.0.0",
    "typescript": "^5.7.3",
    "@types/node": "^22.0.0"
  }
}
