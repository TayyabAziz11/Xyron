# Shared

Shared type definitions and schemas used across web dashboard and backend.

**Status:** Placeholder — to be populated when web dashboard is built.

## Planned Contents
- `types/` — TypeScript interfaces for API payloads
- `schemas/` — JSON schemas for plans, commands, approvals
