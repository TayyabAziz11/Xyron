# AI Operator — Python Dependencies

# FastAPI + server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
python-multipart>=0.0.12
python-dotenv>=1.0.0

# Voice pipeline
faster-whisper>=1.1.0
sounddevice>=0.5.1
numpy>=1.26.0
scipy>=1.13.0

# Text-to-Speech
pyttsx3>=2.90

# System monitoring
psutil>=5.9.0

# HTTP client (for internal API calls)
httpx>=0.27.0

# Gmail API
google-auth>=2.16.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.1.0
google-api-python-client>=2.80.0

# requests — used by src/ai_operator/ (business-automation agents), dev/,
# and scripts/. Already declared in pyproject.toml's [project.dependencies]
# but missing here — requirements.txt is what a fresh install actually
# uses, so a clean checkout was missing a real runtime dependency of code
# main.py imports at startup (found in the 2026 platform-stabilization audit).
requests>=2.28.0

# Feature #9 — Browser control (run: playwright install chromium)
playwright>=1.44.0

# Intent classifier — local semantic matching, no API cost (~90MB model download on first use)
sentence-transformers>=3.0.0
openai>=1.0.0


# Volume control — native Windows COM (works on native Windows Python only; WSL2 uses PowerShell fallback)
# On Windows: pip install pycaw comtypes
# pycaw>=20231107
# comtypes>=1.4.1

# Brightness control — cross-platform DDC/CI + OS APIs (install on the target system)
# On WSL2 + Windows: pip install screen-brightness-control
# screen-brightness-control>=0.23.0

# Wake word detection — local, MIT license, no paid tier needed
# pip install openwakeword
# openwakeword>=0.6.0

# Media key control (native Windows Python only; WSL2 uses PowerShell fallback)
# pip install pynput
# pynput>=1.7.6

# Audio decode for wake-detect endpoint (enables WebM→PCM without ffmpeg)
soundfile>=0.12.1

# Phase 4 — Local TTS + streaming pipeline
# Kokoro ONNX — local high-quality TTS (~82M model, downloaded on first use from HuggingFace)
kokoro-onnx>=0.5.0
# Microsoft cloud TTS fallback (no model download, requires internet)
edge-tts>=7.0.0
chromadb>=1.5.0
ollama>=0.6.0
rapidfuzz>=3.0.0

# Phase 1 — System Intelligence: semantic filesystem index
faiss-cpu>=1.8.0          # vector index for fs semantic search (GPU FAISS optional, CPU is plenty for local fs scale)
watchdog>=4.0.0           # real-time filesystem change detection (PollingObserver — WSL2 DrvFs has no inotify)
pymupdf>=1.24.0           # PDF text extraction (imported as `fitz`)
python-docx>=1.1.0        # DOCX text extraction
openpyxl>=3.1.0           # XLSX text extraction
python-pptx>=0.6.23       # PPTX text extraction
