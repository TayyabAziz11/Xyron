"""
AI Operator — Agent layer.

Agents are autonomous modules that accept a command and coordinate
one or more skills to fulfill it. Each agent owns a domain:

  EmailAgent       — drafts, sends, and replies to emails
  LinkedInAgent    — creates and publishes LinkedIn content
  OdooAgent        — queries and writes to the Odoo ERP
  ReportingAgent   — generates daily/weekly summaries
  IntegrationAgent — checks and reports on integration health
  WorkflowAgent    — orchestrates multi-step workflows
  ApprovalAgent    — manages the human-in-the-loop approval pipeline
"""
