"""
Operator Mode — human-like computer operation layer for Xyron.

Gated behind OPERATOR_MODE=true in backend/.env.
When disabled, the existing voice pipeline is completely unaffected.

Package name: operator_mode (not 'operator' — avoids shadowing Python stdlib)
"""
